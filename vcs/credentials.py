# COPY AND PASTE FROM RESPECTIVE VCS HOST

# Define all hosting services available for use
vcs_hosts = ['github', 'bitbucket']

# Set up github app credentials for global use
github_config = {
    'client_id': '<>',
    'client_secret': '<>'
}

# Set up bitbucket app credentials for global use
bitbucket_config = {
    'client_id': '<>',
    'client_secret': '<>'
}
